{
  "name": "agent-tools",
  "version": "0.1.0",
  "private": true,
  "description": "Skills for Codex, Claude Code, and opencode, plus runtime integrations for Codex and Claude Code.",
  "license": "MIT",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "bin": {
    "agent-tools": "./scripts/install.mjs"
  }
}
