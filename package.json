{
  "name": "agent-tooling",
  "version": "0.1.0",
  "private": true,
  "description": "Reusable agent skills, hooks, statusline tooling, and install helpers",
  "license": "MIT",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "bin": {
    "agent-tooling": "./scripts/install.mjs"
  }
}
